package mongo

// adapterGitVersion is surfaced via the MongoDB buildInfo command.
// Keep it short and stable so users can confirm they're running a binary
// that includes recent fixes (e.g. aggregation stage aliases).
const adapterGitVersion = "mog-adapter+uniq-global+arraycols"
